# bunfig.toml
[install]
# Only install package versions published at least 7 days ago
minimumReleaseAge = 604800 # seconds - it'll allow "3d" too

# These packages will bypass the  minimum age requirement
minimumReleaseAgeExcludes = ["@types/bun", "typescript"]